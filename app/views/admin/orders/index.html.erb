<!-- jumbotron-->
<div class="page-title d-flex" aria-label="Page title" style="background-image: url(img/page-title/shop-pattern.jpg);">
      <div class="container text-left align-self-center">
        <nav aria-label="breadcrumb">

        </nav>
        <h1 class="page-title-heading">Back Office Commandes</h1>
      </div>
    </div>

    <!-- Backoffice Orders-->
<section class="container py-5 mb-4">
  <h1 class="h4 block-title text-center pt-3">Les commandes de mon site CatCatsLand</h1>

          <div class="table-responsive">
            <table class= 'table table-striped'>
            <thead>
            <tr class= 'bg-gradient'>
              <th>#</th>
              <th>Prix total</th>
              <th>Mettre à jour la commande</th>
            </tr>
            </thead>
            <tbody>
            <% @orders.each do |order|%>
            <tr>
              <td><%=order.id%></td>
              <td><%=order.amount%></td>
              <td><%=link_to 'Editer', edit_admin_order_path(order.id) %></td>
              <td><%=link_to 'Supprimer', admin_order_path(order.id), method: :delete %></td>
            </tr>
              <% end %>
            </table>
      </div>
</div>
</div>
</section>